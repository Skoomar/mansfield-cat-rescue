// TODO: maybe just have a link to the SumUp page for MCR for now instead of a page
import { PageHeader } from '@/components/PageHeader';

const Shop = async () => {
    return (
        <>
            <PageHeader>
                <h1>SumUp Shop</h1>
            </PageHeader>
        </>
    );
};

export default Shop;
