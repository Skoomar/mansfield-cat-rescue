// TODO: maybe just have a link to the SumUp page for MCR for now instead of a page
const Shop = async () => {
    return (
        <>
            <h1>SumUp Shop</h1>
        </>
    );
};

export default Shop;
