
// TODO: error message if there's an issue when authenticating or fetching cats (and if the fetch cats returns an empty object)Mac6827


const Adoption = async () => {
    return (
        <>
            <h1>Adoption</h1>
        </>
    );
};

export default Adoption;